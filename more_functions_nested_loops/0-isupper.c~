#include "main.h"
/**
 * _isupper - Checks if a character is uppercase
 * @c : the char to check
 *
 * Return : 1 if c uppercase , 0 otherwise
 */
int _isupper(int c)
{
if (c >= 'A' && 'Z')
return (1);
return (0);
}
